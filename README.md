# LineUp
LineUp is a scheduling tool for when you need 100% coverage over a period of time.

### API client autogeneration
Run `GEN=true dotnet build` in LineUp.Backend to generate the API client in the lineup-client.