**lineup-client**

---

# LineUp Frontend

The Frontend for LineUp uses React, TypeScript, Vite, and pnpm. The frontend will automatically run with the backend with `aspire run`. In order to run the frontend tests, make sure that you have pnpm installed and then run the following:

- `pnpm i`
- `pnpm test`
